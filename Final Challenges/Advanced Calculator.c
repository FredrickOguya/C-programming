/*
Challenge

Write a program that acts as a calculator as in the previous lessons, but now it will ger input from the user until he enters e.
Each input from the user will be separated by a new line(/n).

The calculation will be int he format operation float (separated by space), where the possible operations are +. - ,*,//
Each output (calculation result) should also be separated by a new line.

Check the test cases for examples
*/

#include <stdio.h>

int main(){
    int ch= getchar();
    int opp='e',num1,num2;
    if ()

}
